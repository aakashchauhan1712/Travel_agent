# Core
streamlit==1.46.1
python-dotenv==1.1.1

# LangChain + LangGraph
langchain==0.3.27
langchain-google-genai==2.1.10
langchain-community
langgraph

# Vector Store (for RAG)
faiss-cpu

# HTTP requests (used by weather.py and public-data lookups)
requests

# Data validation (used internally by LangChain)
pydantic
